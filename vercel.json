{
    "build": {
        "env": {
            "DATABASE_URL": "file:./dev.db"
        },
        "src": "your_source_directory",
        "use": "@vercel/node",
        "command": "prisma generate && npm run build"
    }
}